import { calculateContributionAmount } from "../utils/Entry.utils";

describe("Person Service - Test", () => {
  it("should build query", () => {
    console.log(calculateContributionAmount(500));
  });
});
