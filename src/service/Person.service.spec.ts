describe("Person Service - Test", () => {
  it("should build query", () => {
    console.log(
      JSON.stringify({
        host: "localhost",
        user: "root",
        password: "",
        port: 3306,
        database: "cepo_de_oro",
      })
    );
  });
});
