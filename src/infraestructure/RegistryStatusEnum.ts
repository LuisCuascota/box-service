export enum RegistryStatusEnum {
  PAID = "paid",
  CURRENT = "current",
  LATE = "late",
}

export enum EntryBillTypeEnum {
  TRANSFER = "transfer",
  CASH = "cash",
  MIXED = "mixed",
}

export enum AccountStatusEnum {
  OK = "ok",
  LATE = "late",
}
